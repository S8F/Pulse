/**
 * App-level Button atom.
 * Re-exports the shadcn Button with all its variants intact.
 * Feature code imports from here — never from ui/button directly.
 */

export { Button, buttonVariants } from '../../ui/button'
